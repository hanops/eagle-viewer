EAGLE_VAULT_ROOT=/path/to/Design.library
VIEWER_PASSWORD=
VIEWER_SECRET_KEY=
# Optional native-client token. Generate a long random value and never commit it.
VIEWER_API_TOKEN=
# Keep this outside the read-only Eagle library mount.
VIEWER_STATE_PATH=data/eagle-viewer-state.json
